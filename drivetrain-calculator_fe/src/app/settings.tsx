/**
 * Route entry for application settings and future configuration options.
 */

import { SettingsScreen } from '@/features/settings/screens/SettingsScreen';

export default function SettingsRoute() {
  return <SettingsScreen />;
}
