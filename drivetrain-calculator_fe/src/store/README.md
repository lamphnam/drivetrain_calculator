# Store Layer

Reserve this folder for lightweight client state such as session selections, drafts, and persisted calculation summaries. Keep business rules in feature modules, not in global state definitions.
